all:
	#motion-estimation
	#make --directory=Motion_Estimation all
	
	#corner-detection
	make --directory=corner-detection all

	#rotation
	make --directory=Rotation all

	#scaling

	#gaussian-blur

	#high-pass-filter